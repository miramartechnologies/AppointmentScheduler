package Controller;

import java.time.LocalDateTime;

/**
 * Lambda expression for adding time in minutes to a LocalDateTime parameter.
 */
public interface GeneralInterface {
    LocalDateTime addTimeMinutes(LocalDateTime ldtTime);
}
